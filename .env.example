# Read by vite.config.ts in the dev server and proxied to /api/rpc.
# No VITE_ prefix on purpose: prefixed variables get inlined into the bundle and
# would be readable by anyone. This one never leaves the server.
RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-key
