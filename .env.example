# Local convenience only — Vite inlines this into the bundle, so it is NOT a secret.
# For a real secret on Cloudflare, use the /api/rpc proxy and `wrangler pages secret put RPC_URL`.
VITE_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-key
